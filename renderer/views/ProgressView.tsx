import React from 'react';
import { Box, Typography } from '@mui/material';

const ProgressView: React.FC = () => (
  <Box>
    <Typography variant="h5">(Hidden) Stats</Typography>
    <Typography variant="body2" color="text.secondary">Stats are simplified in this build.</Typography>
  </Box>
);

export default ProgressView;
